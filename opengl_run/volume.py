# Load up some zarr array
# Figure out camera bounds in x y and z
# Load up those blocks (plus some extra surrounding)
# keep everything loaded until we start running out of memory then get rid of (earliest? farthest?) blocks
