---
created: 2026-04-18
modified: 2026-05-16
---

# Now — Lucida Current State

Snapshot of what's active. Refresh via the `/repo-wiki` now pass after significant shifts.

## In flight

- **Render-pipeline refactor (PRD #622)** — `gpu.worker.ts` (815 LOC) + `volumeHandlers.ts` (646 LOC) + `sliceHandlers.ts` (554 LOC) split into a tree of focused modules under `lucida-web/src/renderer/` (subdirs `coldState/`, `proxy/`, `volume/`, `slice/`, `worker/`, `descriptor/`). Eleven incremental slices. Mirrors PRD #607 (upload) and PRD #592 (fetch/decode); algorithmic core (`wantedSet`, `proxyAtlas`, `descriptorBuffer`, etc.) already well-tested so test investment is ~60% post-extraction. Two latent bugs fixed mid-refactor: well-as-proxy memberId silently wrong in pool registry (Slice 2) and `removeLayerResources` Map cleanup leak (Slice 8). Wire-protocol field renames `chunksEvicted.datasetId` → `memberId` (Slice 1) + `ColdStateActiveEntry` discriminated union (Slice 11). ADR [[decisions/0035-gpu-worker-split-into-renderer-subdirectories]]. Source design exploration: `wiki/outputs/dechaos-render-2026-05-16/` (8 passes).
- **OME-Zarr expansion** — non-standard input handling (CZI 6D mosaics, `chunk_shape>1` on canonical-indexed `t`/`c`, non-canonical axes). Most recent work targets PRD #451. See [[lucida-store]]; touches [[gotchas/non-canonical-axes]] and [[gotchas/blosc-support]] (both may need refresh — see [[queue]]).
- **`lucida-store` redesign** — PRD #148, server-side chunk serving + storage abstraction.
- **Deployment artifacts and release pipeline (PRD #486)** — single-image Dockerfile + reference k8s manifests + `RUNBOOK.md` + CI/release-please/release workflows + `static_serve` and health endpoints in [[lucida-server]] + `LUCIDA_LOG_FORMAT`/`LUCIDA_WEB_DIST` env vars + env-backing of `--data-dir`/`--proxy-cache-dir`/`--proxy-concurrency` CLI flags + clearing the three pre-existing TS errors in [[gotchas/preexisting-ts-build-errors]]. ADRs [[decisions/0020-single-image-with-servedir]] / [[decisions/0021-deployment-artifacts-as-reference-templates]] / [[decisions/0022-manual-merge-release-please-on-main]] proposed.

## Designed, on hold

(none)

## Recently shipped (since 2026-04-18)

- **Upload-pipeline refactor (PRD #607):** Sixteen commits on a single feature branch (`refactor/upload-pipeline`) landing as one PR (#621). `orchestrator.ts` (2027 LOC, 36 fields, twenty responsibilities split planner-vs-upload) split into a planner-only `Orchestrator` (738 LOC) plus a new `Uploader` (492 LOC) backed by `lucida-web/src/pipeline/upload/` — a directory of 15 focused modules mirroring the shape of `pipeline/fetch/` after PRD #592. Slices in dependency order: docs-prep ADR 0034 + dechaos artifacts + now.md (`c49dcf2`, `b470651`), Slice 0 directory scaffold (`cc24e5a`), Slice 1 pre-refactor characterization tests (~525 LOC across `synthesizeWellRosterEntry` / `computeScissorRect` / `proxyKeyFromX` / chunk-delivery / `handleChunksEvicted` / multi-dataset `it.fails` markers / cold-state lifecycle invariant) (`f2391e6`), Slice 2 mechanical placements (`MissingChunk*Lite` aliases dropped, `proxyKeyFromX` → `proxyKeys.ts`, `UPLOAD_*`/`COLD_STATE_*`/`MAIN_VIEW_UPLOAD_BUDGET_BYTES` → `constants.ts`, `identityMatrix` → `coldState/identity.ts`, `parentWellId?` tightened to `string | null`) (`eb08f29`), Slice 3 drop `workerWantedSet` dead state + fix CHUNK_PIPELINE.md drift (16 MB → 8 MB, ghost filter claim removed) (`b576458`), Slice 4 multi-dataset resend fix — `_lastFilteredRequests`/`_lastProxyRequests`/`_lastEntities`/`_lastVisibleRegion`/`_lastCachedKeyCounts` to per-dataset Maps + `planningState.delete(id)` lifecycle fix; the Slice-1 `it.fails(...)` markers flipped to passing `it(...)` (`32ac799`), Slice 5 `DeliveryTracker` extraction — 4 maps (post-Slice-3) consolidated behind intent-named API; `handleChunksEvicted` shrinks to two-line delegation (`1a0c620`), Slice 6 pure cold-state builders — `buildColdState` + `buildColdActiveEntry` (collapses three near-duplicate variant literals into one branching function), `buildRoster`, `buildViewHotState`, `buildDisplayStateByChannel`, `synthesizeWellRosterEntry` move, `computeScissorRect` move (`369dd5c`), Slice 7 drain/resend/dispatch + `buildManifestByImage` per-tick memo (kills the O(D × I) per-chunk scan in `sendDeliveryToWorker`); `classifyDelivery` + `classifyChunkResend` + `classifyProxyResend` pure verdicts + `runDrainPass`/`runChunkResendPass`/`runProxyResendPass` (`14b25de`), Slice 8 `WorkerFeedback` extraction — handlers shrink to thin delegations (`a77561a`), Slice 9 telemetry modules — `UploadTelemetry` + `ColdStateTelemetry` + shared `SustainedCondition` + `ConsecutiveTickDetector` (each anomaly call site shrinks from ~30 LOC of bookkeeping to a one-line `detector.tick(...)` call) (`64cc8ff`), Slice 10 `Uploader` coordinator wires everything — Orchestrator drops from 1501 LOC to 738; the `Planner` → `Uploader` seam uses Option A (five dedicated methods: `onPlanRebuildStart`, `sendColdState`, `sendViewHotStateIfAdvanced`, `recordPlanForDataset`, `deliverToWorker`) rather than a `PlanTickBundle` umbrella type; `RenderLoop` constructs `Uploader` first then passes it into `Orchestrator`; orchestrator.test.ts (1746 LOC) split into planner-only `orchestrator.test.ts` (555 LOC) + new `uploader.test.ts` (1407 LOC) for the migrated upload-side describes (`8f08d2e`), Slice 11 `UploadClient` interface + `SliceChunk`/`VolumeChunk` consolidation into single `Chunk` type (wire-array shape preserved) (`5b4b331`). Lint fixup: drop runtime `DeliveryTracker` imports from `uploader.test.ts` (only used as type) (`ed5d096`). Phase 5 finalize: new [[upload-pipeline]] subsystem article + refreshed [[chunk-pipeline]] (Uploader credit, 8 MB budget gotcha, DeliveryTracker reference in worker-eviction gotcha) + index.md (`8ea0ce1`). Two latent bugs ride along: `workerWantedSet` dead state in Slice 3 (subtractive — populated never read; doc claimed a filter that didn't exist), and multi-dataset resend under-resend in Slice 4 (flat fields lost all-but-last-dataset state in the planning loop; observable as "second dataset takes longer to recover from a transient eviction storm"). Tests grew from 736 to 880 (+144 across 17 new per-module test files + characterization additions). Chunk/proxy unification explicitly NOT pursued (helper bodies too small; Pass 6 recommendation; differs from PRD #592's deferred Slice 12). ADR [[decisions/0034-orchestrator-split-into-pipeline-upload]]. Subsystem article [[upload-pipeline]] new. Source design exploration: `wiki/outputs/dechaos-upload-2026-05-15/` (8 passes).
- **Fetch/decode subsystem refactor (PRD #592):** Twelve commits on a single feature branch landing as one PR. `cpuCache.ts` (1627 LOC, 35 fields, twelve responsibilities) split into `pipeline/fetch/` directory of focused modules; final `cpuCache.ts` at 1094 LOC (~620 code-only) coordinator. Slices in dependency order: docs-prep ADR 0032 + dechaos artifacts (#593), Slice 0 directory move (#594), Slice 1 pre-refactor tests + ZSTD .buffer fix (#595), Slice 2 mechanical placements — `extractDataType` → `manifestTypes.ts`, `parseProxyHeader`/`proxyResponseKey` → new `wireProtocol.ts`, `lane:"proxy"` dropped, redundant params dropped, `ReadyChunkDelivery.kind` required (#596), Slice 3 `InteractionModeDetector` (#597), Slice 4 `TelemetryCounters` + `BurstLogger` + `imageWireFormats` leak fix (#598), Slice 5 `EvictionPolicy` (`LRUPolicy` + `TieredPolicy` with active-detail tiebreaker preserved exactly) (#599), Slice 6 per-cache stores (`ChunkStore` parameterized for main + overview; `ProxyStore` separate; design Q resolved in favor of parameterized) (#600), Slice 7 `Scheduler<Req>` generic with chunk + proxy as two instances (unification deferred to Slice 12) (#601), Slice 8 typed `FetchError(kind)` + `RetryPolicy` + "no wire format registered" misclassification fix (#602), Slice 9 `RejectionTracker` (#603), Slice 10 thin coordinator audit (extracted `types.ts`; cpuCache.ts 1257 → 1094 LOC) (#604), Slice 11 bridge `handleBinary` cleanup — `BridgeHandlers.onChunkData`/`onProxyData` collapse into `onBinary`; `ContentSource.handleBinary` owns the `proxy/` prefix sniff (#605). Two latent bugs ride along: `imageWireFormats` leak in Slice 4 (dataset removal now calls `ProxiedContentSource.unregisterDataset` from `RenderLoop.removeDataset`), and "no wire format registered" misclassification in Slice 8 (was transient + retry; now `kind: "permanent"`). Slice 1's ZSTD `.buffer` round-trip test surfaced a third bug: `fzstd.decompress(...).buffer` returns the underlying buffer including 12 bytes of garbage prefix — fixed via `.slice(byteOffset, ...)`. Tests grew from 564 to 736 (+172 across new per-module test files + characterization gaps + bug-fix regressions). Validator stays monolithic; per-module unit tests added alongside each extraction (planning refactor pattern per [[decisions/0029-planning-index-split-into-per-concern-files]]). ADRs [[decisions/0032-cpucache-split-into-pipeline-fetch]] and [[decisions/0033-typed-fetch-error]]. Two deferred items: chunk/proxy `Scheduler` unification (until a third asset kind appears) and `ContentSourceFactory` (until `Direct`/`Local` `FetchSource` variants land). Subsystem article [[cpu-cache]] refreshed. Source design exploration: `wiki/outputs/dechaos-fetch-decode-2026-05-15/`.
- **Planning Stage 5 opportunistic (PRD #578):** Three slices (PRs #583, #584, #585) plus a docs-prep PR (#582). Polish layer of the planning subsystem refactor; no user-visible behaviour change. Slice 1 split `pipeline/planning/index.ts` (1695 lines) into 5 sibling files (`types.ts`, `modes.ts`, `chunks.ts`, `emit.ts`, `plan.ts`) with `index.ts` as a barrel re-export — public surface unchanged, intra-module imports flattened. Dropped the `PROMOTE_THRESHOLD_PX` legacy alias (only `planning.test.ts` consumed it). Slice 2 introduced trailing-suffix discipline (`Vox` / `World` / `Px`) on planning contract fields: `BaseEntitySnapshot.position` → `layoutPositionVox` (with an inline comment distinguishing it from `centroidWorld`); `VisibleRegion.xyBounds` / `zRange` / `sortCenter` → `*Vox`. Suffixes applied JS-side at the snapshot boundary; Rust source naming untouched. Same slice introduced `lucida-web/src/axes.ts` (`Axis = { T:0, C:1, Z:2, Y:3, X:4 } as const`) and replaced 67 magic-index sites across orchestrator / layoutBuilders / planning / minimapPath / hooks / debug overlays. Slice 3 added `pipeline/planning/validate.ts` with nine semantic-invariant checks on `plan()`'s inputs, gated by `import.meta.env.DEV` so production builds tree-shake it cleanly (verified zero matches in `dist/`). Per-check coverage in `validate.test.ts`. Notable carve-outs the validator surfaced: visible fields with absent parent wells are legitimate production output (check 1 narrowed); wells use `imageId: ""` by convention (check 3 carves out empties). Check 6 (asset-catalog reference resolution) was withdrawn post-ship in #587 after a real-app trace showed the catalog is flattened across all datasets while the snapshot is per-dataset — they legitimately diverge. PR #588 loosened check 9 to allow `Image` entities to back `FieldEntry` (singletons go through the field code path via `groupByWell`). PR #589 ran a proactive audit of the remaining checks and withdrew check 7 (minimapPending keys) for the same producer-vs-snapshot scope mismatch as check 6 — `minimapPath` keys by all dataset images while `snapshot.entities` is view-query (visible only). Validator has **seven** active checks. Tests grew from 536 to 569 in PRD #578, then settled at 564 after #587/#588/#589. ADRs [[decisions/0029-planning-index-split-into-per-concern-files]] / [[decisions/0030-coordinate-frame-naming-discipline]] / [[decisions/0031-validate-planning-inputs-dev-mode-boundary-check]] all cite [[principles/planning]]. Closes the three-PRD planning refactor arc (PRDs #545, #563, #578).
- **Planning Stage 4 contracts (PRD #563):** Five slices (PRs #570, #571, #572, #573, #574) plus a docs-prep PR (#564). Type-system contract refactor, no user-visible behaviour change. Slice 1 mechanical cleanups (drop `EntitySnapshot.numLevels`; `parentId: string | null` required; `datasetId` carried in `PlanningSnapshot` and stamped by planner at emit time, no orchestrator post-stamp). Slice 2 type relocation (`PlanningEpochs` → `SceneEpochs` in new `pipeline/epochs.ts`; `VisibleRegion` → new `pipeline/viewport.ts`; clean cut, no compat shims; ~21 import sites updated). Slice 3 introduced `PlanningState` as the explicit carry-forward seam: `plan(snapshot, state, config?)` three-way decomposition, `RequestPlan.nextState` opaque pointer pattern. Slice 4 discriminated `ActiveSetEntry` into `WellAsProxyEntry | FieldEntry | InvisibleEntry` with top-level `kind` discriminator; `EntityMode` narrowed; `InvisibleEntry` no longer conflates with visible field-detail mode; new `ResolvedMode` type for the pre-discrimination decision step. Slice 5 (final) discriminated `EntitySnapshot` into `ImageSnapshot | WellSnapshot | FieldSnapshot` (conservative form, `levels` on all three); `parentId: string` lives only on `FieldSnapshot` (non-null); `groupByWell` orphan-field branch removed; `buildPrevModeByWell` simplified. ADRs [[decisions/0026-discriminated-active-set-and-entity-types]], [[decisions/0027-planning-state-as-the-carry-forward-seam]], [[decisions/0028-scene-epochs-rename-and-relocation]] all cite [[principles/planning#4-planning-is-pure-carry-forward-state-is-explicit]]. Tests grew from 523 to 536.
- **Planning module refactor + realtime config panel (PRD #545):** Six slices (PRs #553, #555, #557, #558, #559, #560) plus a docs-prep PR (#552) and a release-please config tweak (#554). Restructured `pipeline/planning.ts` into `pipeline/planning/` (`index.ts`, `config.ts`, `snapshot.ts`, `debug.ts`, `synthetic.ts`, `configStore.ts`) with named primitives composed by a slim `plan()`. Extracted ~120 lines of WASM-snapshot translation out of the orchestrator into `buildPlanningSnapshot`. Introduced `PlanningConfig` typed parameter threaded through `plan()` and a singleton `configStore` with localStorage persistence. New top-level "Config" tab in DebugPanel with sliders for every tunable. Promoted minimap to its own MINIMAP lane at offset 0 so it loads first on dataset open (only intentional behavioural change in the PRD). Dropped the dead +2 LOD buffer + the orchestrator's matching filter step. Added the [[principles/planning|planning principles]] doc as the framework future ADRs cite. ADRs [[decisions/0023-minimap-lane-with-highest-priority]] / [[decisions/0024-catalog-degrade-one-tier-at-a-time]] (ratification) / [[decisions/0025-wells-as-planning-unit]] (ratification). Future-work idea captured at #561 (three-tier residency model).
- **Disabled-auth restoration (PRD #527):** Restored `StubPrincipalExtractor` so `LUCIDA_AUTH=disabled` actually disables auth (matches what [[decisions/0018-auth-mode-auto-detect-by-bind-address|ADR-0018]] promised but slice-2 of PRD #455 silently retired). Removed the dead `/auth/dev/login` machinery, baked `LUCIDA_BIND=0.0.0.0:9876` as a Dockerfile default, and restructured the README Quick Start into four explicit scenarios (local-only / LAN-shared / with sign-in / develop). Per-browser anon identity in disabled mode captured as deferred work in [[decisions/deferred]].
- **Saved views (PRD #454):** URL-as-app-state (`#view=…`) + server-stored named bookmarks (`#b=<id>`) with live cross-peer sidebar updates. Four slices (PRs #478–#481): Slice 1 web-side encoder/applier/urlSync + Copy URL toolbar + loading banner; Slice 2 SQLite `BookmarkStore` + REST API under `/api/bookmarks`; Slice 3 `BookmarkSidebar` + `#b=<id>` open-by-id + `selectedDatasetId` auto-select wrinkle resolved (option c); Slice 4 `ServerMessage::BookmarkChanged` broadcast (first unsequenced ServerMessage variant). Bookmarks are the second persistent state in [[lucida-server]] (after auth's `login_sessions` and `pending_auth`). See [[saved-views]] subsystem article, [[flows/saved-view-recipient-apply]] flow trace, [[gotchas/saved-view-credentials-in-urls]] / [[gotchas/axum-query-multivalue]], ADRs [[decisions/0013-url-as-app-state-for-saved-views]] / [[decisions/0014-local-file-datasets-personal-only-in-saved-views]] / [[decisions/0015-server-stored-bookmarks-and-auth-seam]].
- **Authentication (PRD #455):** Google OAuth via backend-mediated Authorization Code flow, httpOnly session cookies, SQLite-stored sessions, OSS-configurable env-var contract, bind-address auto-detect. Eight slices (PRs #464–#471) plus a Vite-proxy dev workflow fix (PR #472). First persistent state in [[lucida-server]]. Unblocks PRD #454. See [[auth]] subsystem article, [[flows/auth-signin]] flow trace, [[gotchas/oss-config-defaults]] for env-var pitfalls, ADRs [[decisions/0016-backend-mediated-oauth-with-session-cookies]] / [[decisions/0017-configurable-from-day-one-for-oss-release]] / [[decisions/0018-auth-mode-auto-detect-by-bind-address]].
- **OME-Zarr stack:** Blosc decoder + pinned-axis prefix slice for CZI 6D (`90a3dbc`); `lucida-store::codec` extraction with structured per-level binding seed and strict import validation (`b995ae6`); canonical-indexed `t`/`c` `chunk_shape>1` support (`c4be26c`); non-canonical axis handling (`185c429`); 1c manifest shape facts + anomaly check (`ef01e16`).
- **Instrumentation sweep:** planning ([[planning-domain]]) and CPU cache ([[cpu-cache]]) telemetry; debug overlays for chunkGrid + planning, now in 3D (`6b66140`); render-loop + upload-to-GPU telemetry; cold-state hit rate / cause attribution / churn log. Multiple DebugPanel tab rewrites ("Render", "Planning", "Logging").
- **Atlas-rejection resend storm fix** + `drain_waste` filterRatio redefinition (`8bd1c29`).
- **Y-flip in 3D voxel→world** + smarter active-detail eviction (`d8c11fd`).
- **27 pre-existing TS errors cleared** in `lucida-web` (`593eb8d`).
- **Logging conventions** doc refreshed (`cf074d6`); see [[decisions/0012-logging-conventions]] — still violates article guardrails ([[queue]]).
- **Wiki structural refactor** — numbered ADRs (`0001-…` through `0012-…`), `systems/{crates,subsystems}/` split, `topics/` layer added, glossary stub removed.

## Open questions

See [[queue]]. Key ones: PRDs to fold into `wiki/inputs/`, planning threshold rationale, [[decisions/0010-temporal-runway-not-implemented]] still holding, several gotchas may need refresh after recent OME-Zarr work and TS cleanup.

## Source material

- `CHUNK_PIPELINE.md` at repo root — long-form chunk-lifecycle authoritative trace; [[chunk-pipeline]] points at it.
- Project memory at `~/.claude/projects/-Users-austin-code-lucida/memory/MEMORY.md` carries pointers to recent project-state docs.
