# Systems coverage + accuracy audit — 2026-06-25

Scope: the `systems/` tree — 9 crate articles (`wiki/systems/crates/`) and 15 subsystem articles (`wiki/systems/subsystems/`) — cross-checked against current source (ground truth) at repo root `/Users/austin/code/lucida`. Read-only audit; no source or existing wiki articles were modified. Evidence is cited as `file:line` or symbol. Findings below were spot-verified directly against source where load-bearing.

Verdict legend: **accurate** (no material drift) · **drifted** (one or more concrete errors) · **missing-content** (a real feature/model exists but isn't documented).

---

## 1. Crate articles — accuracy/drift table

All 9 crate articles exist and are 1:1 with the workspace members. 5 are accurate; 3 drifted; 1 has a factual count error.

| Article | Verdict | Concrete issues (with evidence) |
|---|---|---|
| `lucida-core.md` | **drifted** | (1) INVARIANT WRONG: "The only other `&mut self` method on `Scene` is `remove_dataset`" is false — `Scene` also has public `&mut self` methods `set_mode_2d`/`set_mode_3d`/`set_mode_fly` (`scene/mod.rs:126`/`135`/`150`) and `pub fn rebuild_derived` (`scene/mod.rs:710`), plus `pub(crate) inner_set_viewport` (`scene/mod.rs:165`). Verified: `grep '&mut self' scene/mod.rs` returns exactly these six. (2) ATTRIBUTION WRONG: the `SetActiveLayout` explicit early-return is in `Scene::apply` (`command.rs:402–415`), not `DocumentState::apply` (which has a separate `SetActiveLayout` arm at `scene/types.rs:985`). The ordering invariant itself is correct. (3) MODULE-MAP DRIFT: `transform.rs` is described as "4×4 voxel transforms"; it actually defines `VolumeTransform` (voxel→normalized-world model/inv_model matrices, `transform.rs:7`), distinct from lucida-content's `VoxelTransform`. (4) MISSING MODULES: `mat4.rs` (`pub(crate)` column-major mat4/vec3 helpers; `lib.rs:7`) and `wasm_log.rs` (`wasm_log!` macro, `set_debug_categories`; `lib.rs:16`) are undocumented. Rest verified accurate (protocol.rs/BookmarkChanged + presence variants, epoch.rs, saved_view shims, apply/epoch/serde-flatten invariants). |
| `lucida-protocol.md` | **accurate** | All six modules verified. `FetchSource` has `Proxied`/`Direct`/`Local` all implemented, each with a descriptor struct (`fetch.rs:7–36`); `WireFormat::{Raw,Lz4,Zstd}` each carry `{ data_type: DataType }` (`fetch.rs:57–60`); `ProxiedImageSpec`, `ProxyKind` re-export, and the invariants all match. |
| `lucida-content.md` | **accurate** | `url.rs` (4 public fns incl. `dataset_url_hash16`), `normalize.rs` (`classify_axes`, `normalize_to_5d`), `transform.rs` (`VoxelTransform` `#[serde(transparent)]` over `AffineTransform`, column-major `matrix()`), id/entity/image/graph/plate all verified. Invariants (5D shapes, canonical axes, `grid_shape` div_ceil, non-empty `source_layouts`) match. |
| `lucida-store.md` | **accurate** (small omissions) | `ALL_DIMS`, `chunk_key_to_store_path`, 512 MB `CachedStore` budget (`handler.rs:154`), import_types, codec/layout/backend verified. MISSING: `coarse.rs` (`pub(crate)`; `SourceCoarseConfig` `coarse.rs:10` + `select_source_coarse_level` `coarse.rs:28` — import-time source coarse-LOD selection) undocumented; `chunk_key_to_store_path_5d` (second public fn, `lib.rs:74`) not mentioned. Both minor (`parse.rs`/`coarse.rs` are `pub(crate)`). |
| `lucida-server.md` | **drifted** (several concrete errors) | (1) MIGRATION TABLE LIST WRONG: wiki lists a workspace migration creating `workspace_dataset_sources`; the actual table is `dataset_sources` (`migrations/20260528000004_create_workspaces.sql:36`). (2) MIGRATION LIST STALE: omits three tables that exist and back features the wiki itself describes — `bearer_tokens` (`...0008`), `cli_token_authorizations` (`...0009`), `workspace_viewer_profiles` (`...0010`). (Also newer migrations `...0011`–`...0013` add saved-view visibility + last-view + proposed index.) (3) STALE LINE REF: gotcha cites `handler.rs:594-619` for the open-dataset binding-race re-check/rebroadcast; that range is now `DatasetHealth` handling — the lost-the-race rebroadcast is at `handler.rs:~2024–2045`, `handle_open_remote_dataset` starts `handler.rs:1449`. (4) ATTRIBUTION IMPRECISE: `LUCIDA_WEB_DIST` is read in `main.rs:492`, not `static_serve.rs` (which only references the var in landing-page text, `static_serve.rs:138`). Default `./lucida-web/dist` is correct. (5) LOCATION IMPRECISE: `migrations/` lives at crate root (`lucida-server/migrations/`), not under `src/`. (6) MISSING: `bookmarks/routes.rs` exists (router builder) but the bookmarks bullet lists only store/handlers/broadcast. Otherwise accurate (13 top-level modules match `lib.rs`; `UnicastRoutes` type, `u64::MAX` sentinel, self-presence filter all verified). |
| `lucida-proxy.md` | **accurate** (minor omissions) | I/O-free split, ProxyKind/ProxyAsset/ProxyHeader/ProxyDtype/ALGORITHM_VERSION, 64-byte header, u16 / `[Z,Y,X]` output, source_content_hash invalidation verified. MISSING from module map: `generate.rs` also exports `estimate_proxy_dims` + `EstimateError` (`generate.rs:89`/`46`), not just `generate_proxy`/`GenerateError`; `header.rs` `source_content_hash` mentioned conceptually, not in the bullet. Minor. |
| `lucida-cli.md` | **accurate** | Top-level `Commands` enum (Status/Server/Auth/Workspace/Dataset/View/Camera/Layer/Channel/Viewer/Peer/Plan/Debug/Layout/SavedView/Admin/Config) matches the noun listing. `dataset montage` flags verified exactly (`--cells` 16, `--cols` 4, `--cell-px` 320, `--json`, `--timeout-seconds` 30; `DatasetCommand::Montage` + `montage.rs`). Module set (incl. `credentials.rs`) matches. Invariants/gotchas consistent. |
| `lucida-py.md` | **accurate** | Only `#[pyclass]` types are `PyScene`/`PyStore` (`lib.rs:13`/`184`); `exclude = ["lucida-py"]` in root `Cargo.toml`; `pymodule lucida` adds both; `ViewportData` is a pure-Python dataclass; package-root imports degrade to `None` on missing extension (`__init__.py:11–20`); `chunk_plan_for` needs a loaded doc; per-store `runtime`; `import_presence` preserves local viewport — all verified. |
| `lucida-web.md` | **drifted** (one factual count error) | SHADER COUNT WRONG: wiki says "three WGSL shaders" (twice). There are **four**, all actively imported: `compositor.wgsl`, `cursors.wgsl`, `slice.wgsl`, `volume.wgsl` (verified via `find lucida-web/src -name '*.wgsl'`; imported by `renderer/{layerCompositor,cursorRenderer,sliceRenderer,volumeRenderer}.ts`). `cursors.wgsl` (peer-cursor rendering) is the likely later addition. Otherwise accurate: 9 subdirs match; `build:wasm`→`lucida-core/pkg/` + `file:../lucida-core/pkg` alias; `applyDocumentCommand`/`applyViewportCommand`; 15 colormaps in `COLORMAP_NAMES`; three render-path entry files; `config/keyBindings.ts`. (Self-describes as a roadmap, so newer top-level files like `axes.ts`/`lastViewPreference.ts` being unlisted is acceptable.) |

**Highest-priority crate fixes**
1. `lucida-server.md` — migration table list (`workspace_dataset_sources` → `dataset_sources`; add `bearer_tokens`/`cli_token_authorizations`/`workspace_viewer_profiles`) and the stale `handler.rs:594-619` ref.
2. `lucida-web.md` — "three" → "four" WGSL shaders.
3. `lucida-core.md` — the false "only other `&mut self` … `remove_dataset`" invariant.

---

## 2. Subsystem articles — accuracy/drift table

All 15 subsystem articles exist. 8 accurate; 6 drifted; 1 (`saved-views.md`) is materially missing a shipped feature model. (`chunk-lifecycle.md` is referenced by the index but is a flow-level overview; not separately audited here beyond confirming it exists as an index target — see Coverage gaps.)

| Article | Verdict | Concrete issues (with evidence) |
|---|---|---|
| `auth.md` | **accurate** | `PrincipalExtractor` trait `principal.rs:89`; all five impls present (`StubPrincipalExtractor:528`, `SessionCookieExtractor:118`, `BearerTokenExtractor:246`, `DualCredentialExtractor:324`, `GoogleJwtPrincipalExtractor:473`); `build_extractor` dispatch (`middleware.rs:158–165`); stub yields `dev@local`/admin unless `lucida_dev_principal` cookie (`dev.rs:20,30–33`); all five stores exist; every audit-event name in the article is actually emitted; CLI flow paths (`main.rs:367,373,430,434`). |
| `cpu-cache.md` | **drifted** | (1) ERROR: decode pool size is **not 3** (hardware-dependent). Article states "(3)" three times and derives "`maxConcurrentFetches` (≈9)". Source: `defaultPoolSize()` = `Math.max(2, Math.floor(cores/2) - 1)` (`pipeline/fetch/decodePool.ts:13`), `new DecodePool()` constructed with no override (`hooks/useBridge.ts:155`), and `maxConcurrentFetches = decode.size * FETCH_CONCURRENCY_MULTIPLIER` with multiplier 3 (`cpuCache.ts:78,176`). Both numbers vary with `navigator.hardwareConcurrency`; "3"/"≈9" hold only on an 8-core machine. (2) MINOR: decode normalization also handles `float32` (`decode.worker.ts:98–127`), undocumented. Rest verified (1230-line file, three stores each with own LRU, `EvictionTier` exactly 3 values, budgets 512/64/256 MB, elastic borrowing, minimap→overviewStore at priority 0). |
| `upload-pipeline.md` | **drifted** (names only; behavior accurate) | MODULE-NAME DRIFT: cold-state builders are split across differently-named files than implied — `buildColdState` in `coldState/build.ts`, `buildViewHotState` in `coldState/hotState.ts`, `buildDisplayStateByChannel` in `coldState/displayState.ts`, `identityMatrix` in `coldState/identity.ts`, `buildRoster` in `coldState/roster.ts`. `delivery/dispatch.ts` exports `dispatchChunkDelivery` (the entry point `uploader.ts:231` actually calls) — the article lists only `dispatchChunk`/`dispatchProxy`. All functions exist; behavior verified accurate (311-line coordinator, 8 MB main budget halved per tier, `markSent` after dispatch, `WorkerFeedback` delegation, `tickCoordinator.ts` 711 lines). |
| `gpu-residency.md` | **accurate** | `gpu.worker.ts` 34 lines; `worker/` six modules; `RendererState` field clusters (`worker/state.ts:25–82`); pool keys include channel+tier (`poolKeys.ts:53–62`); `memberTierKey` routing; `chunkKeys.ts` exports; descriptor layout owned by `descriptor/layout.ts` with named offsets; `coldState/apply.ts` sequence; proxy keying. Trivial: renderers are created lazily (`bootstrap.ts:99–114`), article says "instantiates". |
| `worker-protocol.md` | **accurate** | Worker→main union exactly 5 variants (`workerProtocol.ts:530–535`); Main→worker union incl. 4 minimap messages (`:418–434`); chunk data carries `memberId`+`tier?`; render multipass stale-tolerant vs chunk/proxy stale-rejected; `ColdStateActiveEntry` discriminated union (`:330–356`). Minor: `ColdStateActiveEntryBase` field list (article line 55) omits `detailOwnedLodRange`/`wellProxyAvailable`/`layoutPositionVox` — incomplete, not wrong. |
| `generated-coarse.md` | **drifted** | ERROR: `Ready(bytes)` is attributed to the wrong enum. Article says `GeneratedChunkStatus` has a "5th variant `Ready(bytes)` … used internally." Source: `GeneratedChunkStatus` has exactly 5 **unit** variants `Pending`/`Unavailable`/`FailedTransient`/`FailedPermanent`/`Ready` — none carries bytes (`lucida-protocol/src/generated.rs:56–62`). The `Ready(Vec<u8>)` payload belongs to a different enum, `DerivedChunkLookup` (`lucida-server/src/generated.rs:218–224`), which `serve_generated_chunk_request` matches on (`handler.rs:2515`). The two enums are conflated. Rest verified (binary frame layout, status text message, `GeneratedAvailabilityUpdate`, max-pool `max-pool-v1`, zero-fill, cache_identity versions). |
| `minimap.md` | **accurate** | Dual-path feed (planning `lane:"minimap"` at `emit.ts:73` + independent `tickMinimapOverview` draining CPU cache, `minimapPath.ts:174–274`); per-member overview texture; explicit `coarse_level_index` pointer (`minimapPath.ts:37–45`); per-channel LUT+contrast binding so 2D matches 3D; `MINIMAP_LANE_OFFSET=0` highest priority; separate GPU resources removed on cleanup. `renderer/minimapHandlers.ts` exists. |
| `multichannel-and-colormaps.md` | **drifted** | (1) ERROR: gamma exponent is `gamma`, not `1/gamma`. Article step 3 says `pow(t, 1/gamma)`; shaders apply `pow(clamp(...), gamma)` directly (`slice.wgsl:304`, `volume.wgsl:469`), gamma stored un-inverted (`descriptorBuffer.ts:393`). (2) CONSEQUENT ERROR: the gamma-0 gotcha ("explodes … `pow(t,1/0)`") is wrong — `pow(t,0)=1.0` (flat wash-out), no divide-by-zero. (3) ERROR: `ensure_channel` is a method on `DatasetDisplaySettings`/`ChannelSettings` in `scene/types.rs:106`, not `command.rs` (the `SetChannel*` handlers that call it are `command.rs:743–784`). (4) MINOR: LUT sample is `textureSampleLevel(...)` (`slice.wgsl:305`), not `textureSample`. Rest verified (5-field `ChannelSettings`, 15 `Colormap` variants in order, `BlendMode` 3 variants + defaults, 6 `ViewportCommand` channel variants each bumping `epochs.selection`, channel count from level-0 `shape[1]`, compositor does not blend in-shader). |
| `planning-domain.md` | **accurate** | Module layout (types/modes/chunks/emit/plan/index + config/configStore/snapshot/proxyResidency/debug/synthetic/validate) all present; `plan(snapshot, state, config)` signature (`plan.ts:69–71`); priority formula + lane offsets match the **default** constants in `config.ts` and the live code reads tunable `config.*` (`emit.ts:34–38,75`) — article acknowledges "live-tunable". Minor: lane table omits `PROXY_LANE_OFFSET=1000`. `Axis = {T:0,C:1,Z:2,Y:3,X:4}` (`axes.ts:14`), `validatePlanningInputs` verified. |
| `scene-state-and-epochs.md` | **accurate** | `SceneEpochs` = 6 `u64` counters (content/layout/view/selection/asset/annotation) `epoch.rs:8–29`; per-epoch command mappings correct against `command.rs` — notably `RenameDataset` bumps `content` (`command.rs:498–505`), where the wiki is *more* current than the stale `epoch.rs` doc-comment. `DocumentState` `scene/types.rs:460` composed via `#[serde(flatten)]` (`scene/mod.rs:75–76`); `rebuild_derived` `scene/mod.rs:710`; server owns per-workspace `seq`. |
| `layout-system.md` | **accurate** | `RegisterLayout`/`SetActiveLayout` semantics, dedupe-by-id, unknown-id fallback all verified by test name (`register_layout_dedupes_by_id` `command.rs:1997`, `unknown_layout_id_is_no_op_for_derived` `command.rs:4749`); `reanchor_for_layout` `scene/types.rs:905`; `document.registered_layouts`/`active_layout_ids` fields; `build_grid_field_transforms` `plate.rs:114`. |
| `presence-and-follow-mode.md` | **accurate** (stale framing) | Five wire messages (`protocol.rs:106–125`); transitive-chain resolution + loop prevention (`session.rs:219–261`); disconnect resets followers (`session.rs:173–177`); `Steer`→`set_follow` (`handler.rs:558–571`); throttles 50/200/50 ms (`bridge.ts:471,489,568`). STALE FRAMING (not a hard error): describes one global `Session`; presence/follow is now **per-workspace** — each `LiveWorkspace` owns its `Session` (`handler.rs:44,51`). Mechanics still correct. |
| `saved-views.md` | **drifted / missing-content** | (1) MISSING: the entire saved-view visibility / proposed-views model (#702). Source has `SavedViewVisibility { Shared, Personal, Proposed }` (`workspace.rs:92–96`) with routes `/saved-views/{id}/visibility`, `/approve`, `/reject`, `/viewer-profiles/{profile}` (`workspace.rs:4135–4147`) and migrations `...0011`/`...0013`. Article documents only a flat editor-create/viewer-read model. Largest subsystem gap. (2) STALE: workspace saved-views route list (article lines 57–64) omits the visibility/approve/reject/viewer-profiles routes. (3) STALE: "legacy `BookmarkSidebar`" — `BookmarkSidebar.tsx` no longer exists (only orphaned `.css` + `showBookmarkSidebar` state name remain in `App.tsx`); the live sidebar is `WorkspaceSavedViewsSidebar` (`App.tsx:1517`). (4) MISSING (minor): `SavedView` is now embedded in `Annotation::view` (`saved_view.rs:62–63`), a new consumer. Accurate parts: capture-record schema, encoder/applier/urlSync, `BookmarkChanged` unsequenced design, local-file sharp edge, `dataset_volume_shape` carries Z but not T/C (`wasm.rs:393`, `applier.ts:730–780`), server clears `SavedView.datasets` before persist (`workspace.rs:988,4014`). The server-side bookmarks subsystem is still live and mounted (`main.rs:515`) — that section is not stale. |
| `workspaces.md` | **drifted** | (1) ROUTE WRONG (article line 8): "addressed … at `/ws/workspaces/:id`" — the browser page route is `/w/:id` (`WorkspaceRoot.tsx:17` regex `^/w/([^/]+)/?$`). `/ws/workspaces/{workspace_id}` is the **WebSocket** endpoint (`workspace.rs:4186` → `workspace_ws`). The article correctly says `/w/:id` at line 10 — internally inconsistent. (2) IMPRECISE (line 14): `ds-{16-hex BLAKE3 of the canonicalized URL}` — actually `format!("ds-{:016x}", …)` over the **first 8 BLAKE3 bytes** as LE u64 (`url.rs:120,129–133`); 16 hex chars from 8 hash bytes, not 16. (A separate 16-byte `dataset_url_hash16` exists for the proxy cache.) `deployment.md` overlaps — see below. Rest accurate and carries the workspace refactor well (`wds-{uuid}`, `unique(workspace_id, dataset_source_id)` + `ON CONFLICT DO NOTHING`, never-leak 404/410, per-workspace `LiveWorkspace.session`). |
| `deployment.md` | **accurate** | Single image; `static_serve.rs` reads `LUCIDA_WEB_DIST` (default correct); health probes `/healthz`+`/readyz` (`health.rs:37–42`); backend scheme dispatch `gs://`/`s3://`/`http(s)`/local (`backend.rs:140,22,131`); `PrincipalExtractor` seam (`principal.rs:89`); `/var/lib/lucida`, `ReadWriteOnce`, `strategy: Recreate` (`extras/deploy/k8s/deployment.yaml:37`); all referenced manifests exist. Minor: doesn't surface newer `LUCIDA_WORKSPACE_IDLE_TTL_SECS`/`LUCIDA_WORKSPACE_IDLE_SWEEP_SECS` (`main.rs:401–402`), but explicitly defers the full env reference to `gotchas/oss-config-defaults`. |

**Highest-priority subsystem fixes**
1. `saved-views.md` — add the visibility/proposed-views model (`Shared`/`Personal`/`Proposed`) and its routes; correct the removed-`BookmarkSidebar` reference.
2. `multichannel-and-colormaps.md` — gamma is `pow(t, gamma)` not `pow(t, 1/gamma)`; rewrite the gamma-0 gotcha; move `ensure_channel` to `scene/types.rs`.
3. `cpu-cache.md` — decode pool size and `maxConcurrentFetches` are hardware-dependent, not the fixed "3"/"≈9".
4. `generated-coarse.md` — un-conflate `GeneratedChunkStatus` (unit `Ready`) from `DerivedChunkLookup::Ready(Vec<u8>)`.
5. `workspaces.md` — fix the `/ws/workspaces/:id` page-route error (it's the WS endpoint; page is `/w/:id`).

---

## 3. Coverage gaps — undocumented crates/subsystems/modules that need an article

No crate is undocumented (all 9 have articles). Gaps are concentrated in web-internal and cross-cutting subsystems.

### Gap 1 — Annotations / mentions / comment-threads (HIGHEST PRIORITY)
**What:** A large, genuinely cross-cutting collaboration subsystem with its own command vocabulary, epoch, geometry/anchoring model, share-by-link security invariants, and a mentions/notification layer. **No dedicated article anywhere.** Only scattered one-line mentions inside `lucida-web.md` (saved-views parenthetical), `lucida-server.md` (parenthetical), and a single `epoch.rs` bullet in `lucida-core.md`. `topics/collaboration.md` does not mention annotations at all.

**Where:**
- lucida-core: `Annotation`, `AnnotationKind` (Point/Line/Box), `Comment` (`scene/types.rs:167–256`); `AddAnnotation`/`AddComment` document commands (`command.rs:111+`); dedicated epoch counter `epochs.annotation` (`epoch.rs:28`); projection math `project_annotation`/`pick_annotation_voxel`/`annotation_world_point` (`scene/mod.rs:582–698`); jump-to-mention arcball mechanism (#526, `scene/mod.rs:1786+`).
- lucida-server: persistence via `DocumentState`/`persist_document`; REST create path threading `visibility`; duplication semantics; the security-critical NEVER-LEAK / `Annotation::view` strip-embedded-datasets invariants (`workspace.rs:5907+`, `9233+`, `9593+`).
- lucida-web: ~8,200 lines across 39 source+test files — `AnnotationOverlay.tsx` (1459), `AnnotationOverlay3D.tsx` (750), `ThreadPopover.tsx` (854), `MentionsOfMe.tsx` (407), `annotationParticipants.ts`, `annotationMentions.ts`, `annotationGeometry.ts`, `useMentionAutocomplete.ts`; deep-linking `hooks/useAnnotationDeepLink.ts`, `hooks/useViewedMentions.ts`; `savedView/{buildAnnotationView,restoreAnnotationView,annotationDeepLink}.ts`.

**Why:** Largest undocumented web subsystem; a coherent feature spanning all three tiers with non-obvious share-by-link security invariants — exactly the tribal knowledge the wiki exists to capture. Recommend `systems/subsystems/annotations.md`, linked from `topics/collaboration.md`.

### Gap 2 — Camera / 3D navigation + key-bindings / input (HIGH PRIORITY)
**What:** The camera model and the input/keybinding registry that drives 3D navigation. Documented only as two one-line file descriptions in `lucida-core.md` (lines 36–37); no subsystem article; the web config surface is entirely undocumented.

**Where:**
- lucida-core: `camera.rs` is 1,842 lines — `Slice` (2D), `Arcball` (3D orbit), `Fly` (first-person); `ClipMode`, `VisibleRegion`, `project_to_screen`/`unproject_ray`/`fly_tick`.
- lucida-web: `config/keyBindings.ts` (centralized action→key map: `fly.forward/...`, `camera.toggleFly`, `clip.increase/decrease`; `ActionName`, `getBoundKeys()`, `isActionPressed()`) — referenced by nothing in the wiki; `hooks/useKeyState.ts`, `hooks/useFlyCameraInput.ts` (163), `components/FlyCameraHint.tsx`, `components/FocalDepthControl.tsx`.

**Why:** Orbit/fly/clip navigation, the keybinding registry, and focal-depth form one user-facing subsystem with a real config surface and shared core math. Recommend `systems/subsystems/camera-and-navigation.md` (or split camera-model vs. input/key-bindings).

### Gap 3 — 3D chunk-spawn focal depth (recent feature, fold in)
**What:** Shipped in commit `54ebdc4` ("configurable 3D chunk-spawn focal depth", #532). Not mentioned in `planning-domain.md` or anywhere.

**Where:** `App.tsx`, `components/FocalDepthControl.tsx`, `pipeline/planning/config.ts`, `debug/ConfigTab.tsx`.

**Why:** It is a planning-input knob. At minimum fold into `planning-domain.md`; best covered as part of the camera/navigation article (Gap 2).

### Gap 4 — Debug panel / overlays / config UI (MEDIUM PRIORITY)
**What:** The in-app developer control surface for planning config and residency/telemetry overlays. Name-dropped across ~12 articles but has no article of its own.

**Where:** `lucida-web/src/debug/` (~4,860 lines) — `DebugPanel.tsx` (1923), `DebugOverlays.tsx` (1267), `ConfigTab.tsx` (433, the live planning-config editor), `debugStats.ts` (458), `radiusPreview.ts`.

**Why:** A coherent developer-facing subsystem (live config editing + residency/telemetry visualization). Weaker than Gaps 1–2 but a real candidate: `systems/subsystems/debug-panel.md`.

### Non-gaps (investigated, adequately covered — do not add articles)
- Workspace dashboard / routing — covered by `workspaces.md` (`WorkspaceDashboard.tsx` `/`, `WorkspaceRoot.tsx` `/w/:id`, `workspaceApi.ts`). Note: routing is hand-rolled (no `react-router`).
- Decode/codec layer — covered by `lucida-store.md` (`codec.rs`), `lucida-server.md` (`decode.rs`/`decode/blosc.rs`), `gotchas/blosc-support.md`, `topics/storage-and-import.md`. (3,851-line `blosc.rs` only summarized, but the concept is documented.)
- lucida-store ingest binary — covered by `lucida-store.md` (`ingest/`, `main.rs` Convert/Plate). Thin but present.
- Web auth — `auth.md` covers it (UnauthLanding, `whoami` 200/401, Vite proxy origin wrinkle).
- App.tsx / bridge.ts / renderLoop.ts orchestration — distributed across `lucida-web.md`, `decisions/`, `flows/`, `gotchas/app-tsx-hook-order.md`.

---

## 4. Removable / over-documented

Nothing in `systems/` is fully obsolete or warrants deletion. Items below are mis-scoped descriptions, not removable articles.

- **`systems/index.md` line 23 — stale "legacy" framing for lucida-proxy.** The index calls lucida-proxy "historical/legacy pure-compute proxy generation algorithm." This is misleading: the crate is actively wired as the proxy fallback path and is load-bearing (`lucida-server` `handler.rs`, `proxy/generator.rs`, `proxy/cache.rs`, `proxy/server_source.rs` use `generate_proxy`/`write_header`/`read_header`/`ALGORITHM_VERSION`). The article body itself (`lucida-proxy.md:8`) correctly says "Fallback path, still wired" — only the one-liner in the index is stale. Fix: drop "historical/legacy" from `index.md:23`. (Flagged for the owner; read-only here.)
- **`minimap.md` (smallest article, 2505 bytes)** is proportionate to its ~1,100 lines of code and is accurate — not over- or under-documented. No action.
- No subsystem article was found redundant against another. The `bookmarks` server subsystem coexists with workspace saved views (both live and mounted), so neither's documentation is removable on grounds of obsolescence.

---

### Appendix — overlap to resolve (not a gap, not removable)
`workspaces.md` and `deployment.md` both touch the WebSocket endpoint and env contract; the `/ws/workspaces/:id` page-vs-WS confusion (§2, workspaces) is the concrete symptom. When fixing the workspaces route error, ensure the two articles agree that `/w/:id` is the page and `/ws/workspaces/{id}` is the socket.
