ADRs cite [principles](../principles/index.md) as their justification when applicable. Principles never cite back — they remain agnostic to which decisions exist today.

# Decisions

ADR-style records of architectural choices. Numbered sequentially in the order they were captured. Each ADR records *that* a decision was made and *why* — not a fully-templated breakdown. Optional `Status` / `Considered Options` / `Consequences` sections appear only when they add genuine value.

Most articles below were originally seeded by reading the code (rationale reconstructed). Where a decision has subsequently been confirmed, refined, or extended via authoritative source material or a maintainer interview, the article notes that.

## Articles

- [Document vs Viewport Command Split](0001-document-vs-viewport-split.md) — disjoint `DocumentCommand` / `ViewportCommand` enums separate shared/sequenced from local/ephemeral
- [Peer-to-Peer Follow Mode](0002-peer-to-peer-follow-mode.md) — anyone can follow anyone; server flattens chains into stars
- [All GPU Work on a Dedicated Web Worker](0003-gpu-on-dedicated-worker.md) — all WebGPU runs in `gpu.worker.ts` via `OffscreenCanvas` transfer
- [Multi-Pool Atlases by (Dataset, Channel, Chunk Dims)](0004-multi-pool-atlases.md) — historical proxy atlases keyed by `(dataset, kind, slotDims, channel)` for collection FPS; superseded for the coarse/detail path by ADR 0039
- [Three-Output Import Model](0005-three-output-import-model.md) — `ImportResult` splits manifest, fetch, binding seed by audience
- [ContentSource (JS) vs FetchSource (wire)](0006-content-source-vs-fetch-source.md) — JS-side `ContentSource` wraps wire-side `FetchSource`
- [WASM Scene as Source of Truth](0007-wasm-scene-as-source-of-truth.md) — Scene state lives in WASM; JS is a thin orchestration layer
- [CpuCache as Sole Fetch Path](0008-cpu-cache-as-sole-fetch-path.md) — `SharedChunkQueue` deleted in S5; `CpuCache` is the only path
- [Pull-Based RAF Render Loop with Typed Dirty Flags](0009-pull-based-raf-with-typed-dirty.md) — RAF loop with `interactiveDirty` (immediate) and `residencyDirty` (33ms throttle)
- [GPU-Side Temporal Lookahead — Won't Implement](0010-temporal-runway-not-implemented.md) — GPU-side runway not pursued; CPU-side runway + scrubbing eviction is sufficient (2026-04-17)
- [Dual Hand-off on DatasetOpened (WASM + JS)](0011-dual-handoff-on-dataset-opened.md) — `DatasetOpened` event splits into WASM `apply_command` and JS `setupFetchPipeline`
- [Logging Conventions](0012-logging-conventions.md) — `tracing` spans on the server, `bridgeLog` helper on the client, `dot.scope` event names (2026-04-20)
- [URL-as-App-State for Saved Views](0013-url-as-app-state-for-saved-views.md) — saved views are debounced URL-hash writes (Google-Maps-style); refresh preserves view; sharing = copy URL (2026-05-07; accepted 2026-05-08)
- [Local-File Datasets Are Personal-Only in Saved Views](0014-local-file-datasets-personal-only-in-saved-views.md) — local-file paths in saved views work for sender refresh but warn on share; no auto-conversion to served URLs (2026-05-07; accepted 2026-05-08)
- [Server-Stored Bookmarks and the AuthPrincipal Seam](0015-server-stored-bookmarks-and-auth-seam.md) — SQLite-backed bookmarks; `AuthPrincipal` trait abstracts the auth provider; side-table over JSON1 for any-overlap query (2026-05-07; accepted 2026-05-08)
- [Backend-Mediated OAuth with Session Cookies](0016-backend-mediated-oauth-with-session-cookies.md) — Google OAuth flow runs server-side; httpOnly session cookie; no JWT in JS; SameSite=Lax + REST discipline for CSRF (2026-05-08, proposed)
- [Configurable From Day One for OSS Release](0017-configurable-from-day-one-for-oss-release.md) — every organization-specific value lives in env vars; `PrincipalExtractor` trait is the OSS provider extension point (2026-05-08, proposed)
- [Auth Mode Auto-Detect by Bind Address](0018-auth-mode-auto-detect-by-bind-address.md) — `LUCIDA_AUTH` defaults derived from bind address; loopback → disabled, non-loopback → google; `LUCIDA_INSECURE=1` overrides (2026-05-08, proposed)
- [Post-Logout Marker Cookie + `prompt=select_account`](0019-post-logout-marker-cookie-and-prompt-select-account.md) — `lucida_signed_out` marker cookie set by `/auth/logout`; middleware serves a static landing instead of auto-bouncing; `/auth/start` adds `prompt=select_account` and clears the marker (2026-05-08)
- [Single-Image Container with `ServeDir` is the Canonical Deploy Unit](0020-single-image-with-servedir.md) — `lucida-server` serves the SPA via `tower-http::ServeDir`; production deploy unit is a single container image bundling API + SPA (PRD #486; 2026-05-13, proposed)
- [Deployment Artifacts Are Reference Templates, Not Opinionated Infra](0021-deployment-artifacts-as-reference-templates.md) — `extras/deploy/` ships raw YAML with `<PLACEHOLDER>` values; no Helm chart, no Kustomize overlay, no provider-specific resources upstream (PRD #486; 2026-05-13, proposed)
- [Trunk-Based Releases via Manual-Merge `release-please` on `main`](0022-manual-merge-release-please-on-main.md) — trunk-based releases via `release-please` on `main` with manual-merge of the release PR; tag push triggers multi-arch image build to ghcr.io (PRD #486; 2026-05-13, proposed)
- [Minimap Lane with Highest Priority](0023-minimap-lane-with-highest-priority.md) — minimap is its own lane at offset 0 (highest priority); other lanes renumbered upward; minimap fetches first and evicts last (PRD #545; 2026-05-14)
- [Catalog Degradation Steps One Tier at a Time](0024-catalog-degrade-one-tier-at-a-time.md) — catalog-aware mode degradation steps exactly one tier; tier-skipping forbidden; ratification of an existing invariant (PRD #545; 2026-05-14)
- [Groups Are the Planning Unit in Collections](0025-groups-as-planning-unit.md) — in collections, all tiles of one group agree on a single mode; per-tile divergence within a group is out of scope; ratification of an existing invariant (PRD #545; 2026-05-14)
- [Discriminated Active-Set and Entity Types](0026-discriminated-active-set-and-entity-types.md) — `ActiveSetEntry` and `EntitySnapshot` become discriminated unions with top-level `kind`; per-variant invariants compile-time enforced (PRD #563; 2026-05-15)
- [`PlanningState` as the Carry-Forward Seam](0027-planning-state-as-the-carry-forward-seam.md) — `PlanningState` separates across-tick state from per-tick snapshot; planner returns `nextState` opaquely (PRD #563; 2026-05-15)
- [`SceneEpochs` Rename and Relocation](0028-scene-epochs-rename-and-relocation.md) — `PlanningEpochs` → `SceneEpochs` in `pipeline/epochs.ts`; `VisibleRegion` → `pipeline/viewport.ts`; no compat shim (PRD #563; 2026-05-15)
- [`planning/index.ts` Split into Per-Concern Files](0029-planning-index-split-into-per-concern-files.md) — `pipeline/planning/index.ts` (1695 lines) splits into `types.ts` / `modes.ts` / `chunks.ts` / `emit.ts` / `plan.ts`; `index.ts` becomes a barrel; 5 files (not 6 — constants already in `config.ts`); `emit.ts` (not `priority.ts`) since the bulk is the four lane emitters (PRD #578; 2026-05-15)
- [Coordinate-Frame Naming Discipline at the JS↔WASM Boundary](0030-coordinate-frame-naming-discipline.md) — trailing-suffix discipline (`Vox` / `World` / `Px`) on planning contract fields + `Axis.{T,C,Z,Y,X}` namespace constants for the TCZYX 5D layout; JS-side at the snapshot boundary only (PRD #578; 2026-05-15)
- [`validatePlanningInputs` as the Dev-Mode Boundary Check](0031-validate-planning-inputs-dev-mode-boundary-check.md) — `validatePlanningInputs(snapshot, state)` runs seven semantic-invariant checks at `plan()` entry; gated by `import.meta.env.DEV`; throws on violation (no degrade); originally nine, checks 6 and 7 withdrawn post-ship for producer-vs-snapshot scope mismatch (PRD #578; 2026-05-15)
- [`cpuCache.ts` split into `pipeline/fetch/` modules](0032-cpucache-split-into-pipeline-fetch.md) — `cpuCache.ts` (1627 lines) splits into a `pipeline/fetch/` directory of focused modules with `cpuCache.ts` as a ~250 LOC coordinator; mirrors [`planning/index.ts` Split into Per-Concern Files](0029-planning-index-split-into-per-concern-files.md) in shape; integration test suite stays monolithic; two latent bugs ride along inside the slices that surface them; chunk/proxy `Scheduler` unification deferred (PRD #592; 2026-05-15)
- [Typed `FetchError` + injectable `RetryPolicy` at the fetch boundary](0033-typed-fetch-error.md) — `ContentSource` raises typed `FetchError(kind: "permanent" | "transient" | "pending" | "abort")` at every rejection site; `CpuCache.fetchAndDecode` dispatches via `classifyFetchError` + injected `RetryPolicy` (`OnceTransientRetry` / `NeverRetry`); fixes the "no wire format registered" misclassification surfaced by dechaos pass 5 and lets generated coarse pending statuses avoid failure tracking (PRD #592, issue #602; 2026-05-15)
- [`orchestrator.ts` split into `pipeline/upload/` modules](0034-orchestrator-split-into-pipeline-upload.md) — `orchestrator.ts` (2027 lines) splits into a planner-only `Orchestrator` (~400 LOC) plus a new `Uploader` coordinator (~250 LOC) backed by `pipeline/upload/` — mirrors [`cpuCache.ts` split into `pipeline/fetch/` modules](0032-cpucache-split-into-pipeline-fetch.md) in shape; integration test suite stays monolithic through Slice 9; two latent bugs ride along (`workerWantedSet` dead state, multi-dataset resend under-resend); chunk/proxy asset-abstraction explicitly NOT pursued (PRD #607; 2026-05-16)
- [`gpu.worker.ts` split into `renderer/` subdirectories](0035-gpu-worker-split-into-renderer-subdirectories.md) — `gpu.worker.ts` (815 lines) + `volumeHandlers.ts` (646 lines) + `sliceHandlers.ts` (554 lines) split into a tree of focused modules under `lucida-web/src/renderer/` (subdirs `coldState/`, `proxy/`, `volume/`, `slice/`, `worker/`, `descriptor/`); eleven incremental slices; mirrors [`orchestrator.ts` split into `pipeline/upload/` modules](0034-orchestrator-split-into-pipeline-upload.md) in cadence and shape; two latent bugs ride along (group-as-proxy memberId in pool registry, `removeLayerResources` Map cleanup leak); wire-protocol renames `chunksEvicted.datasetId` → `memberId` + `ColdStateActiveEntry` discriminated union; no `RenderClient` interface split (no second renderer on the horizon); does NOT cite a principle (`principles/render-pipeline.md` deferred to post-refactor INTERVIEW, same as `cpu-cache.md` / `upload-pipeline.md`) (PRD #622; 2026-05-16)
- [Descriptor byte-layout single source of truth + WGSL ↔ TS lock test](0036-descriptor-byte-layout-ssot-and-wgsl-lock-test.md) — `EntityDescriptor` byte layout owned by a single TS file (`renderer/descriptor/layout.ts`) as named offset constants; companion `layout.test.ts` parses the `EntityDescriptor` struct from both `slice.wgsl` and `volume.wgsl` and asserts agreement, failing CI on drift; WGSL codegen explicitly deferred (only two shaders; build cost not justified); generalizable seam for future cross-language byte-shape contracts (PRD #622, Slice 3; 2026-05-16)
- [Delivery state as a CpuCache sidecar](0037-delivery-state-as-cpucache-sidecar.md) — `CpuCache` owns optimistic chunk/proxy sent state via `DeliveryState`; `getDeliverable()` replaces `ready[]` drain plus chunk/proxy resend passes; uploader planner-staging hooks disappear; send order becomes strict priority across deliverables (PRD #640; 2026-05-17)
- [Budgeted proxy GPU residency](0038-budgeted-proxy-gpu-residency.md) — historical proxy-path decision superseded for the default coarse/detail path by ADR 0039; proxy GPU residency becomes a planning-owned, worker-global budgeted desired set over coherent group bundles (PRD #664; 2026-05-18)
- [Chunk-only coarse/detail residency](0039-chunk-only-coarse-detail-residency.md) — fallback/residency becomes two canonical chunk tiers (`coarse` and `detail`); detail defaults to source level 0; proxy fallback and group-as-residency-unit behavior are superseded for the new path (PRD #672; 2026-05-18)
- [Generated coarse as derived pyramid levels](0040-generated-coarse-as-derived-pyramid-levels.md) — coarse points at an existing source level when possible, otherwise at an append-only server-managed derived level served through the normal chunk path with metadata/readiness deltas (PRD #672; 2026-05-18)
- [Clean two-source chunk-tier renderer](0041-clean-two-source-chunk-tier-renderer.md) — the renderer binds two explicit chunk tier sources (`detail`, `coarse`) in one draw path, with clean tier descriptors instead of proxy binding reuse (PRD #672; 2026-05-18)
- [Canonical dataset URL form](0042-canonical-dataset-url-form.md) — every dataset URL passes through one string-level normalization step at the storage boundary (drive-letter lowercased, slashes forward, `file://` stripped, UNC forward-slashified); single canonical form drives `DatasetId` hashing, proxy-cache dir, wire, and display; helpers live in new `lucida-content::url` module; full filesystem canonicalize rejected (PRD #703; 2026-05-26)
- [Sunset dispositions for the three superseded server surfaces](0043-superseded-server-surfaces-sunset.md) — end-states for the pre-workspace generations: **retire** the global `/ws` session (zero shipped consumers; no "default workspace" bridge), **converge** bookmarks into workspace saved views (`#b=` redefined as workspace saved-view id; SQLite rows kept), **delete** the proxy/asset fallback protocol including the `lucida-proxy` crate (no supported path reaches it: the server gate defaults off and the client side is a hidden debug-panel toggle; generated coarse doesn't use it) — with verified consumer lists and per-subject deletion inventories (2026-07-04, accepted)

## Deferred — considered but not built yet

Design ideas we explored and decided to hold for later live in
[Deferred — considered but not built yet](deferred.md). Each entry inline-sketches the idea and links to the
ADR that establishes the relevant context.
