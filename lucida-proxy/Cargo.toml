[package]
name = "lucida-proxy"
version = "0.2.0"
edition = "2024"
license = "MIT"

[dependencies]
lucida-content = { path = "../lucida-content" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
thiserror = "1"
