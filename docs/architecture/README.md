# Architecture Docs

This folder contains implementation-oriented architecture references.

## Files

1. `component-map.md`: current and planned component boundaries.

## Source of Truth Boundary

1. Product intent lives in `SPEC.md` and `specs/roadmap/`.
2. Protocol contracts live in `protocol/` and `docs/protocol/README.md`.
3. Context status and traceability live in `docs/context/`.

Use this folder for architecture-level shape and dependency boundaries, not for rewriting product requirements.
