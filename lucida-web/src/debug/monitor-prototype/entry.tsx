/** PROTOTYPE — throwaway entry for /monitor-prototype.html. Issue #892. */

import { createRoot } from "react-dom/client";
import { MonitorPrototype } from "./MonitorPrototype.tsx";

createRoot(document.getElementById("root")!).render(<MonitorPrototype />);
