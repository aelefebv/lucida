[project]
name = "lucida"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fsspec>=2024.2.0",
    "zarr>=2.18.0,<4",
]

[project.optional-dependencies]
s3 = [
    "s3fs>=2024.2.0",
]
gcs = [
    "gcsfs>=2024.2.0",
]
