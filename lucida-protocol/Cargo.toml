[package]
name = "lucida-protocol"
version = "0.2.0"
edition = "2024"
license = "MIT"

[dependencies]
lucida-content = { version = "0.2.0", path = "../lucida-content" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
