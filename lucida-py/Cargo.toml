[package]
name = "lucida-py"
version = "0.1.0"
edition = "2024"

[lib]
name = "lucida"
crate-type = ["cdylib"]

[dependencies]
lucida-core = { path = "../lucida-core" }
pyo3 = { version = "0.24", features = ["extension-module"] }
serde_json = "1"
